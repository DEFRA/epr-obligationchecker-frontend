@using Microsoft.AspNetCore.Mvc.TagHelpers

@model LanguageSwitcherModel

@if (Model.SupportedCultures!.Count > 1)
{
    @* Temporary removal of language which will be reinstated *@

    @* <ul class="language-toggle">
        @foreach (var culture in Model.SupportedCultures!)
        {
            <li class="language-toggle__option">
                @if (Model.CurrentCulture!.Name == culture.Name)
                {
                    @SharedLocalizer[culture.NativeName]
                }
                else
                {
                    <a asp-controller="Culture" asp-action="UpdateCulture" asp-route-culture="@culture.Name" 
                       asp-route-returnUrl="@Model.ReturnUrl" class="govuk-link govuk-link--no-visited-state">@SharedLocalizer[culture.NativeName].Value</a>
                }
            </li>
        }
    </ul> *@
}
else
{
    @* <div class="language-toggle govuk-!-padding-bottom-8"></div>  *@
}